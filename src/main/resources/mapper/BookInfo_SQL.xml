<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.orphan.bookInfo.mapper.BookInfoMapper">
	<!-- 도서 목록 리스트 조회 -->
	<select id="selectBookInfoList" parameterType="hashMap" resultType="hashMap">
		/*selectBookInfoList*/
		select
			*
		from
			book_info a
		where open_yn = 'Y'
		order by book_seq desc
		LIMIT #{offset},#{limit}
	</select>

	<!-- 도서 목록 카운트 -->
	<select id="selectBookInfoListTotCnt" resultType="int">
		/*selectBookInfoListTotCnt*/
		select count(*) totcnt
		from book_info a
		where open_yn = 'Y'
	</select>
</mapper>